version: 1.0.0
events:
  build:
    steps:
      - hello:
          type: script
          script:
            - echo "Hello, Pipelines!" > docroot/hello.txt
	    - echo "Job ID: $PIPELINE_JOB_ID" >> docroot/hello.txt

