version: 1.0.0
events:
  build:
    steps:
      - hello:
          type: script
          script:
            - echo $PIPELINE_JOB_ID > docroot/hello.txt
      - composer:
          type: script
          script:
          # Run composer install to setup the project and modules.
               - composer install --no-progress
