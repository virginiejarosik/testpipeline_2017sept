diff --git a/includes/bootstrap.inc b/includes/bootstrap.inc
index 744fc8f..2123a80 100644
--- a/includes/bootstrap.inc
+++ b/includes/bootstrap.inc
@@ -1036,7 +1036,7 @@ function variable_del($name) {
  *   The cache object, if the page was found in the cache, NULL otherwise.
  */
 function drupal_page_get_cache($check_only = FALSE) {
-  global $base_root;
+  global $base_root, $da_site;
   static $cache_hit = FALSE;
 
   if ($check_only) {
@@ -1044,6 +1044,10 @@ function drupal_page_get_cache($check_only = FALSE) {
   }
 
   if (drupal_page_is_cacheable()) {
+    $root = $base_root;
+    if ($da_site) {
+      $root = $base_root . '/' . $da_site;
+    }
     $cache = cache_get($base_root . request_uri(), 'cache_page');
     if ($cache !== FALSE) {
       $cache_hit = TRUE;
diff --git a/includes/common.inc b/includes/common.inc
index 20cc82b..42dab65 100644
--- a/includes/common.inc
+++ b/includes/common.inc
@@ -5,7 +5,7 @@
  * Common functions that many Drupal modules will need to reference.
  *
  * The functions that are critical and need to be available even when serving
- * a cached page are instead located in bootstrap.inc.
+ * a cached page are instead located in bootstrap.inc.drupal_page_set_cache
  */
 
 /**
@@ -5208,7 +5208,12 @@ function _drupal_bootstrap_full() {
  * @see drupal_page_header()
  */
 function drupal_page_set_cache() {
-  global $base_root;
+  global $base_root, $da_site;
+
+  $root = $base_root;
+  if ($da_site) {
+    $root = $base_root . '/' . $da_site;
+  }
 
   if (drupal_page_is_cacheable()) {
 
diff --git a/index.php b/index.php
index 8b83199..2f71862 100644
--- a/index.php
+++ b/index.php
@@ -17,5 +17,6 @@
 define('DRUPAL_ROOT', getcwd());
 
 require_once DRUPAL_ROOT . '/includes/bootstrap.inc';
+require_once DRUPAL_ROOT . '/sites/all/modules/ewave/domain_subfolder/domain_subfolder.inc';
 drupal_bootstrap(DRUPAL_BOOTSTRAP_FULL);
 menu_execute_active_handler();
